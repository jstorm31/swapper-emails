## Agent skills

### Issue tracker

Issues live in Linear (team Swapper, key `SW`), distinguished by the `emails` label. See `docs/agents/issue-tracker.md`.

### Triage labels

Default five-role vocabulary, same names as Linear labels. See `docs/agents/triage-labels.md`.

### Domain docs

Single-context. No `CONTEXT.md` or `docs/adr/` yet. See `docs/agents/domain.md`.
